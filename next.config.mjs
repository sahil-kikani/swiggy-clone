/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media-assets.swiggy.com', 'b.zmtcdn.com', 'media-assets.swiggy.com'],
    },
};

export default nextConfig;
